import java.awt.*;import BreezyGUI.*;public class TheGUIPart extends GBFrame{	private Label user; 	private Label pass;	private Label firstname;	private Label lastname;			private TextField username; 	private TextField password; 		private Button login;   TheGUIPart ()   	{	   	user = addLabel ("username",1,1,1,1);  		pass = addLabel ("password",2,1,1,1);  				username = addTextField ("Username",1,2,1,1);		password = addTextField ("Password",2,2,1,1);				login = addButton ("login",3,1,1,1);   	}	public void buttonClicked (Button buttonObj)	{		String userInput = username.getText();		String passInput = password.getText();					if (userInput.equalsIgnoreCase("NathanSears") && passInput.equals("rocks")){			      Frame frm = new TheGUIPart2 ();    			      frm.setSize (300, 300);  			      frm.setVisible (true);   			 			}			else username.setText(""); password.setText("");		}	}